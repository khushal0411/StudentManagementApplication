<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.student_management_system">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Student_Management_System">
        <activity
            android:name=".Auth.Activity.AdminLogIn"
            android:exported="false" />
        <activity
            android:name=".HomeScreen.Activity.HomeScreen"
            android:exported="false" />
        <activity
            android:name=".Auth.Activity.TeacherLogIn"
            android:exported="false" />
        <activity
            android:name=".SplashAndIntro.Activity.IntroScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Auth.Activity.StudentLogIn"
            android:exported="false" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>